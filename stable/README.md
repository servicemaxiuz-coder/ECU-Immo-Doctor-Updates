# Stable channel

The active signed manifest will be published as `stable/update.manifest` only when the first update package is release-qualified.

Release asset: `ECUImmoDoctor-<version>-build<build>-win-x64.zip`

Release tag: `v<version>-build<build>`

The manifest URL is stable; its signed payload points to an immutable, versioned GitHub Release asset URL.
